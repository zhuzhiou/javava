spring:
    application:
        name: live-services
    profiles:
        active: prod
    cloud:
        inetutils:
            ignored-interfaces:
                - Microsoft.*
                - Software.*
                - VMware.*
server:
    port: 8084
eureka:
    instance:
        lease-expiration-duration-in-seconds: 10
        lease-renewal-interval-in-seconds: 10